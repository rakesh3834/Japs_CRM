{
  "name": "japs-crm-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix frontend run build && npm --prefix frontend run build:worker && node scripts/prepare-sites.mjs",
    "dev:api": "node --env-file-if-exists=.env scripts/dev-worker.mjs",
    "test": "node --test tests/*.test.js"
  }
}
